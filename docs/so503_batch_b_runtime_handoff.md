# SO503 batch B renderer handoff

Batch B models are static review-only Xacro assets. The LCMS model is one
composite object (LC + SQD2 visuals); S4 has three explicit static variants.
Keep zero-joint robots renderable and route unknown joints to diagnostics. Test
with synthetic UUID-scoped frames; do not require a live Bioyond producer/TCP.
